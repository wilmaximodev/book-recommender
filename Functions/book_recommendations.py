def recommend_fiction(user_id: int) -> str:
    """
    Recomenda livros de ficção ao usuário.
    """
    # Simula a recomendação
    return f"Usuário {user_id}, recomendamos os últimos lançamentos de ficção para você!"

def recommend_doc(user_id: int) -> str:
    """
    Recomenda livros de documentários ao usuário.
    """
    # Simula a recomendação
    return f"Usuário {user_id}, temos ótimos livros de não ficção que podem lhe interessar!"

def recommend_science(user_id: int) -> str:
    """
    Recomenda livros de ciência ao usuário.
    """
    # Simula a recomendação
    return f"Usuário {user_id}, confira os novos títulos na categoria de ciência!"

def recommend_horror(user_id: int) -> str:
    """
    Recomenda livros de terror ao usuário.
    """
    # Simula a recomendação
    return f"Usuário {user_id}, temos ótimos livros de terror que podem lhe interessar!"

def recommend_romantic(user_id: int) -> str:
    """
    Recomenda livros de romance ao usuário.
    """
    # Simula a recomendação
    return f"Usuário {user_id}, confira os novos títulos na categoria de romance!"